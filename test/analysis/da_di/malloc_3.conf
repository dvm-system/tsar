name = malloc_3
plugin = TsarPlugin

sample = $name.c
options = -print-only=da-di -print-step=3 -fno-analyze-library-functions
run = "$tsar $sample $options"
      "$tsar $sample $options -fignore-redundant-memory | -check-prefix=REDUNDANT"

