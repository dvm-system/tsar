name = pointer_1
plugin = TsarPlugin

sample = $name.c
options = -print-only=da-di -print-step=4
run = "$tsar $sample $options | -check-prefix=SAFE"
      "$tsar $sample $options -fignore-redundant-memory=strict | -check-prefix=REDUNDANT"

