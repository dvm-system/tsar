name = inline_class_2
plugin = TsarPlugin
action = init

suffix = tfm
sample = $name.cpp
sample_diff = $name.$suffix.cpp
options = '-clang-inline -output-suffix=$suffix'
run = 'tsar $sample $options'

