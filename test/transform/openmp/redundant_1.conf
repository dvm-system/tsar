plugin = TsarPlugin

suffix = tfm
sample = $name.c
sample_diff = $name.$suffix.c
options = -clang-openmp-parallel -fignore-redundant-memory=strict -output-suffix=$suffix
run = "$tsar $sample $options"

