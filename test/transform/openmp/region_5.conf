name = region_5
plugin = TsarPlugin

suffix = tfm
sample = $name.c
sample_diff = $name.$suffix.c
options = -clang-openmp-parallel -output-suffix=$suffix -foptimize-only=parallel
run = "$tsar $sample $options"

