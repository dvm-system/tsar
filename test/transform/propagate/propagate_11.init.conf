name = propagate_11
plugin = TsarPlugin
action = init

suffix = tfm
sample = $name.cpp
sample_diff = $name.$suffix.cpp
options = '-clang-copy-propagation -output-suffix=$suffix'
run = 'tsar $sample $options'

