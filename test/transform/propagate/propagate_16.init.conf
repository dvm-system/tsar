name = propagate_16
plugin = TsarPlugin
action = init

suffix = tfm
sample = $name.c
sample_diff = $name.$suffix.c
options = '-clang-copy-propagation -output-suffix=$suffix'
run = 'tsar $sample $options'

